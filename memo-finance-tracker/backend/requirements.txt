fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.0
alembic>=1.13.0
pydantic[email]>=2.6.0
python-dateutil>=2.9.0
pytesseract>=0.3.10
Pillow>=10.0.0
# NumPy 2.x manylinux wheels are built with an x86-64-v2 baseline, which crashes
# on older CPUs and on VMs using a generic CPU model (e.g. QEMU/Proxmox
# "qemu64"): "NumPy was built with baseline optimizations (X86_V2) but your
# machine doesn't support (X86_V2)". Pin to the 1.26 line, whose wheel uses the
# universal x86-64 baseline and runs everywhere. opencv must match NumPy's 1.x
# ABI, so pin it to the last release built against NumPy 1.x.
numpy>=1.26.0,<2
opencv-python-headless==4.9.0.80
# Qwen2.5-VL is a vision model: it needs the modern multimodal (mtmd) stack and
# the Qwen25VLChatHandler, which only exist in newer llama-cpp-python than the
# old 0.3.4. Stay on the 0.3.x line for the NumPy 1.x / CPU-baseline reasons
# above; it is built from source in the Dockerfile so it adapts to the host CPU.
llama-cpp-python>=0.3.8,<0.4
rapidfuzz>=3.0.0
python-multipart>=0.0.9
paho-mqtt>=2.0.0
